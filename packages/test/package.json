{
  "name": "test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "prebuild": "rm -rf dist",
    "build": "esbuild index.ts --target=node14 --format=cjs --platform=node --outdir=dist",
    "postbuild": "cp -r ./node_modules dist/node_modules && zip -r dist/app.zip dist/index.js dist/node_modules"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-lambda": "^1.0.7",
    "chrome-aws-lambda": "^10.1.0",
    "puppeteer-core": "^10.4.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.119"
  }
}
