{
  "name": "app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf dist && esbuild index.ts --bundle --sourcemap --platform=node --target=es2020 --outdir=dist",
    "zip": "zip -r ./dist/app.zip ./dist/index.js ./node_modules",
    "b2": "esbuild index.ts --bundle --platform=node --target=node14 --outdir=dist",
    "z2": "zip -r ./dist/app.zip ./dist/index.js",
    "z": "zip -r ./dist/app.zip ./index.js ./node_modules"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "chrome-aws-lambda": "^10.1.0",
    "puppeteer-core": "^10.4.0"
  }
}
